apiVersion: v1
kind: Service
metadata:
    name: cm-usuarios-auth
    namespace: apis
spec:
    ports:
        - name: http
          port: 8080
          protocol: TCP
    selector:
        app: cm-usuarios-auth
